# src/repositories/ — Data Access Layer (Repository pattern).
# Repositories handle all database queries. The service layer calls these
# instead of writing SQL directly, keeping concerns separated.